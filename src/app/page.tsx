'use client';

import { NextPage } from 'next';

import MainLayout from '@/chat/chat-layout';

const HomePage: NextPage = () => {
  return (
    <div>
      <MainLayout />
    </div>
  );
};

export default HomePage;
